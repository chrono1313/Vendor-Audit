# Vendor Audit runtime dependencies.
#
# Minimum versions chosen for the features actually used:
#   - dnspython 2.4+   resolver.timeout/lifetime semantics, current dnssec module
#   - httpx 0.25+      stable HTTP/2 support, sync client, connection-pool fixes;
#                       speaks HTTP/2 by default to match what we ask vendors to do
#   - tldextract 5.0+  bundled PSL with regular updates, modern Python support
#
# Upper bounds intentionally not pinned — these libraries are stable and
# follow semver. If a future major release breaks something, pin then.

dnspython>=2.4
httpx[http2]>=0.25
tldextract>=5.0
idna>=3.7
